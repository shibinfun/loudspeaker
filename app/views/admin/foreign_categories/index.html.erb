<div>
<%= link_to("添加一类",new_admin_foreign_category_path,:class => "btn btn-default")%>

</div>
<div>
<%= link_to("返回文章列表",admin_foreigns_path,:class => "btn btn-default")%>

</div>
<table class = "table table-bordered">
<% @foreign_categories.each do |foreign_category| %>
<tr>
<td>
<%= link_to(foreign_category.name)%>
</td>
<td>
<%= link_to("更改",edit_admin_foreign_category_path(foreign_category))%>
<%= link_to("删除",admin_foreign_category_path(foreign_category),:method => :delete, :data => {:confirm => "你确定？"})%>

</td>
<td>
<%= foreign_category.created_at %>
</td>


  </tr>
<% end %>

  </table>
